## **DOCKER COMPOSE**

Una imagen genera un contenedor y el contenedor es el que se ejecuta

![[Pasted image 20251104171451.png]]

Para meter directamente a: docker stop $(docker ps -q)

Todos los repositorios de maven se quedan guardados en la capeta .m2

## **LOMBOK**
Una libreria es un fichero con un conjunto de clases (extension .jar que significa **Java ARchive** ). Hacen una determinada funcionalidad

**Lombok es una biblioteca para Java que reduce el código repetitivo mediante anotaciones que generan automáticamente métodos como getters, setters, constructores y más.**

🧩 ¿Qué es Lombok y para qué sirve?

**Project Lombok** es una biblioteca que se integra en el compilador de Java para _generar automáticamente código repetitivo_ durante la compilación. Esto incluye:

- **Getters y setters** (`@Getter`, `@Setter`)
- **Constructores** (`@NoArgsConstructor`, `@AllArgsConstructor`, `@RequiredArgsConstructor`)
- **Método `toString()`** (`@ToString`)
- **Método `equals()` y `hashCode()`** (`@EqualsAndHashCode`)
- **Builders** (`@Builder`)
- **Logging simplificado** (`@Slf4j`, `@Log4j2`)
- **Variables inmutables** (`@Value`)
- **Control de excepciones** (`@SneakyThrows`)
  
Esto permite que el código sea más limpio, legible y fácil de mantener.

⚙️ ¿Cómo se usa Lombok?

1. **Instalación en Maven:**
```xml
<dependency>
  <groupId>org.projectlombok</groupId>
  <artifactId>lombok</artifactId>
  <version>1.18.30</version> <!-- versión actual -->
  <scope>provided</scope>
</dependency>
```

2. **Ejemplo de uso:**
```java
import lombok.Data;

@Data
public class Persona {
   private String nombre;
   private int edad;
}
```
- Esto genera automáticamente los métodos `getNombre()`, `setNombre()`, `getEdad()`, `setEdad()`, `toString()`, `equals()`, y `hashCode()`.8. **Configuración en IDEs:**

- En **Eclipse**: instalar el plugin Lombok y añadirlo al archivo `.ini` del IDE
- En **IntelliJ IDEA**: instalar el plugin desde el marketplace y habilitar la anotación de procesadores.

📚 Ventajas clave

- **Ahorro de tiempo**: menos código repetitivo.
- **Mayor legibilidad**: clases más limpias.
- **Facilidad de mantenimiento**: menos errores humanos.
- **Compatible con Maven, Gradle y múltiples IDEs**



@AllArgsConstructor
@Getter
@Setter
@ToString
@Data
etc...
