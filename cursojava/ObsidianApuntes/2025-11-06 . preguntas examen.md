????

Ver variables locales y globales (inicializacion)
ArrayIndexOutOfBoundsException y NullPointerException. ver StringIndexOutOfBoundsException
Pasos por valor y por referencia

Todas las Excepciones heredan de la clase Throwable 

git manual:
git add ./
git status
git commit -m "Mensaje del commit"
git push


CREAR UN USUARIO EN BBDD:

🧵 1. Crear el usuario

CREATE USER nombre_usuario IDENTIFIED BY tu_contraseña;

- `nombre_usuario`: el nombre del nuevo usuario.
- `tu_contraseña`: la contraseña segura que le asignas.

🛡️ 2. Asignar privilegios básicos

GRANT CONNECT, RESOURCE TO nombre_usuario;

- `CONNECT`: permite iniciar sesión.
- `RESOURCE`: permite crear objetos como tablas, vistas, etc.

======== Primer paso para conectarse a una bbdd en java ========
El primer paso para conectarte a una base de datos (BBDD) en Java es **cargar el driver JDBC adecuado** para el tipo de base de datos que vas a usar (MySQL, PostgreSQL, SQLite, etc.).

============= En el examen va a haber que crear una tabla, insertar datos, consultar datos y conectar con java ====================

En el examen va a entrar MAVEN y conceptos de Maven.
Tambien GIT desde linea de comandos.
SQL (no entra lo de Roberto).

---
Integridad referencial SQL

---
rutas absolutas y relativas

---
dibujo de checked y unchecked

---
Interfaz Closeable() y AutoCloseable()

---
try with resources

---
Varargs:
public void test (String nombre, int ... numeros ){

}
Se le puede pasar nada, 1 numeroo  muchos numeros. Ejemplo: test("Eva", 1, 3, 8, 11, 28); , pero tambien se podria: test("Eva"); o tambien se podria: test("Eva", 1); etc

Los **varargs** (argumentos variables) permiten que un método reciba **cero o más argumentos** del mismo tipo, sin necesidad de definir múltiples sobrecargas.

public void test(String nombre, int... numeros) { System.out.println("Nombre: " + nombre); for (int num : numeros) { System.out.println("Número: " + num); } }

✅ Ejemplos válidos de uso

test("Eva"); // Sin números test("Eva", 1); // Un número test("Eva", 1, 3, 8, 11, 28); // Varios números

🔍 Reglas importantes

- Solo puede haber **un parámetro varargs** por método.
- Debe ser el **último parámetro** en la lista.
- Internamente, el varargs se trata como un **array** (`int[]` en este caso).

🧠 ¿Cuándo usarlo?
Ideal cuando no sabes cuántos argumentos se van a pasar, como en funciones de suma, logging, validación, etc.

---
Clase StringBuilder → con .append() no crea nuevos objetos al concatenar 2 de ellos
Clase StringBuffer se utiliza cuando hay hilos

---
propagacion de excepciones

---
Strings y funciones de Strings




