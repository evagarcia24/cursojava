Las excepciones están hechas para notificar fallos que harían que el programa no funcionase correctamente.

Cuando en una clase hay un método abstracto, es obligatorio que la clase sea abstracta. Pero se puede hacer una clase abstracta sin que haya métodos abstractos

**Un método abstracto en Java es una declaración sin implementación, usada para definir comportamientos que deben ser especificados por las subclases. Solo puede existir dentro de una clase abstracta.**

Aquí tienes un resumen claro y completo con lo esencial:

🧠 ¿Qué es un método abstracto?

- **Es un método sin cuerpo**, solo tiene la firma (nombre, tipo de retorno, parámetros).
- Se declara con la palabra clave `abstract`.
```java
  public abstract void hacerSonido();
```

  📦 ¿Dónde se usa?

- **Solo dentro de clases abstractas**, que también se definen con `abstract`.
- Las clases abstractas **no se pueden instanciar directamente**.
- Las subclases que heredan deben **implementar todos los métodos abstractos**, a menos que también sean abstractas.

🧩 ¿Para qué sirve?

- **Define un contrato**: obliga a las subclases a implementar ciertos métodos.
- **Facilita el polimorfismo**: puedes tratar objetos de distintas subclases como si fueran del tipo abstracto.
- **Promueve la reutilización y organización del código**.

🛠️ Diferencias clave con interfaces:
![[Pasted image 20251110174530.png]]

## **JDBC (Java Database Connectivity)**

Es una API de Java que permite a las aplicaciones interactuar con bases de datos relacionales de manera estándar y eficiente.
Necesitamos la libreria de la BBDD e importante: la version correcta

🧩 ¿Qué es JDBC?

JDBC proporciona un conjunto de interfaces y clases que permiten:

- Conectarse a una base de datos
- Ejecutar consultas SQL
- Recuperar y manipular resultados
- Administrar transacciones

🔧 Componentes clave de JDBC

- **DriverManager**: Gestiona los controladores de base de datos.
- **Connection**: Representa una conexión activa con la base de datos.
- **Statement / PreparedStatement**: Permite ejecutar sentencias SQL.
- **ResultSet**: Contiene los resultados de una consulta SQL.
- **SQLException**: Maneja errores relacionados con la base de datos.

Para conectarnos a una BBDD en JDBC necesitamos:
- Librería y Driver
- URL de conexion
- Usuario y contraseña
## TRY WITH RESOURCES

Es necesario implementar la interfaz Autocloseable();

🧩 ¿Qué es try-with-resources?

Es una estructura introducida en **Java 7** que permite **gestionar automáticamente recursos** como archivos, sockets o conexiones a bases de datos. Se asegura de que estos recursos se **cierren correctamente**, incluso si ocurre una excepción.

✅ Requisitos

- El recurso debe implementar la interfaz **`AutoCloseable`** (o `Closeable`, que es una subinterfaz).
- Se declara dentro del paréntesis del bloque `try`.

```java
try (BufferedReader lector = new BufferedReader(new FileReader("archivo.txt"))) {
   String linea;
   while ((linea = lector.readLine()) != null) {
    System.out.println(linea);
   }
} catch (IOException e) {
   e.printStackTrace();
}
```

🔍 **¿Qué hace esto?**

- Abre el archivo `archivo.txt`.
- Lee línea por línea.
- Al finalizar (o si ocurre un error), **`lector.close()` se llama automáticamente**, sin necesidad de un bloque `finally`.

🛠️ Ventajas

- **Código más limpio**: no necesitas cerrar manualmente los recursos.
- **Menos errores**: evita fugas de recursos.
- **Compatible con múltiples recursos**: puedes declarar varios separados por `;`.

