## **Escribir y leer ficheros**
Ver ECLIPSE

## **FRAMEWORK HIBERNATE**

Hibernate es un framework ORM (mapeo objeto-relacional / Object-Relational Mapping ) **para Java que simplifica el acceso a bases de datos mediante el mapeo de objetos Java a tablas relacionales.** Es ampliamente usado en aplicaciones empresariales por su capacidad para reducir el código repetitivo y mejorar la portabilidad entre sistemas de bases de datos.

🧩 ¿Qué es Hibernate?

Hibernate es un _framework de mapeo objeto-relacional (ORM)_ que permite a los desarrolladores trabajar con bases de datos usando objetos Java en lugar de escribir directamente SQL. Fue desarrollado por Red Hat y está distribuido bajo la licencia Apache 2.0.

🚀 Ventajas clave

- **Elimina el uso directo de JDBC**: Hibernate gestiona la conexión, ejecución de consultas y manejo de excepciones, reduciendo el código repetitivo.
- **Portabilidad entre bases de datos**: Puedes cambiar de MySQL a PostgreSQL, por ejemplo, sin reescribir tu lógica de acceso a datos.
- **Lenguaje HQL (Hibernate Query Language)**: Permite escribir consultas orientadas a objetos, más intuitivas que SQL tradicional.
- **Caché de primer y segundo nivel**: Mejora el rendimiento al evitar consultas innecesarias.
- **Integración con JPA (Jakarta Persistence API)**: Hibernate es la implementación más popular de esta especificación.

![[Pasted image 20251113195408.png]]

```JAVA
Session session = sessionFactory.openSession();
Transaction tx = session.beginTransaction();

Employee emp = new Employee();
emp.setName("Eva");
emp.setDepartment("Tech");

session.save(emp);
tx.commit();
session.close();
```

Hibernate tiene 3 capas:
	· Capa de BBDD
	· Capa de servicios
	· Capa de presentacion

**Capa de Base de Datos (Persistencia)**

- **Responsabilidad:** Interactuar con la base de datos.
- Hibernate vive aquí como herramienta ORM.
- Incluye:

- Entidades (`@Entity`)
- Repositorios o DAOs
- Configuración de `SessionFactory`, `EntityManager`, etc.

- Ejemplo:

@Entity public class Cliente { @Id private Long id; private String nombre; }

**Capa de Servicios (Lógica de Negocio)**

- **Responsabilidad:** Aplicar reglas de negocio, coordinar operaciones.
- Invoca los DAOs para acceder a los datos.
- Ejemplo:

public class ClienteService { private ClienteDAO clienteDAO; public void registrarCliente(Cliente c) { // Validaciones, lógica de negocio clienteDAO.save(c); } }


. **Capa de Presentación (Interfaz de Usuario)**

- **Responsabilidad:** Mostrar datos y capturar acciones del usuario.
- Puede ser web (JSP, JSF, Spring MVC) o escritorio (JavaFX, Swing).
- Nunca accede directamente a Hibernate; usa la capa de servicios.
- Ejemplo:

 Registrar

🧠 ¿Y la arquitectura interna de Hibernate?

Hibernate internamente tiene componentes como:

- **Configuration**: carga el archivo `hibernate.cfg.xml`.
- **SessionFactory**: crea sesiones para interactuar con la BBDD.
- **Session**: representa una conexión con la base de datos.
- **Transaction**: gestiona transacciones.
- **Query/Criteria**: ejecuta consultas.

