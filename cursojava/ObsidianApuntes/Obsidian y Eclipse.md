## Obsidian

Control + P (Escribir today y crea nota con la fecha del dia que estamos).

Para poner codigo : escribir `code` y abajo ```java (aqui iria el codigo) y abajo. Ejemplo: ```

`code`
```java
List<Persona> listaPersona = new ArrayList();
```

## Atajos de Eclipse

Control  + Shift + F : Coloca el codigo
Control  + Shift + O : Importa un paquete cuando se necesita
main + control + espacio: crea el método main (public static void main(String[]args){})
syso + control + espacio: escribe System.out.println();
ALT + SHIFT + T: se abre buscador de clases.
ALT + SHIFT + A : borra por tabulaciones, no obligatorio ir borrando linea a linea.
ALT + SHIFT + Y : cambiar todo a minuscula todo lo seleccionado
ALT + SHIFT + X : cambiar todo a mayuscula todo lo seleccionado

